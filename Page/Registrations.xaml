<UserControl x:Class="My_finances.Page.Registrations"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:My_finances.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"      
      Background="Transparent">
    <UserControl.DataContext>
        <local:RegistrationsVM/>
    </UserControl.DataContext>

    <Grid
        Margin="200,100"
        Background="Bisque">
        <StackPanel Margin="50,10,50,0"
                    Background="#FFC8BEBE" Height="140" VerticalAlignment="Top">
            <TextBox Margin="30,10" Text="{Binding Name}"/>
            <TextBox Margin="30,10" Text="{Binding Password}"/>
            <Button Margin="30,20"
                    Content="Next"/>
        </StackPanel>


    </Grid>
</UserControl>
